@model ZYC.VerbClass.Web.Pages.Academic.CourseDefinitionEditorModel
@{
    var submitUrl = Model.IsCreate
        ? Url.Page(null, "Create")
        : Url.Page(null, "Update", new { courseDefinitionId = Model.CourseDefinitionId });
    var cancelUrl = Url.Page(
        null,
        "View",
        Model.IsCreate
            ? new { courseDefinitionId = Model.ReturnCourseDefinitionId }
            : new { courseDefinitionId = Model.CourseDefinitionId }
    );
}

<form method="post"
      hx-post="@submitUrl"
      hx-target="#course-definitions-content"
      hx-swap="innerHTML"
      class="course-definition-editor">
    <input type="hidden" asp-for="ReturnCourseDefinitionId"/>

    @if (Model.HasErrors)
    {
        <div class="course-definition-editor__summary">
            @if (Model.SummaryErrors.Count == 0)
            {
                <div>Please correct the highlighted fields.</div>
            }
            else
            {
                <ul>
                    @foreach (var error in Model.SummaryErrors)
                    {
                        <li>@error</li>
                    }
                </ul>
            }
        </div>
    }

    <div class="course-definition-editor__body form-layout">
        <section class="form-section">
            <div class="form-section__title">Catalog</div>
            <div class="form-grid form-grid--two">
                <div class="field">
                    <label class="field__label" asp-for="Code"></label>
                    <input asp-for="Code" autocomplete="off"/>
                    @foreach (var error in Model.GetErrors(nameof(Model.Code)))
                    {
                        <div class="course-definition-editor__field-error">@error</div>
                    }
                </div>

                <div class="field">
                    <label class="field__label" asp-for="Name"></label>
                    <input asp-for="Name" autocomplete="off"/>
                    @foreach (var error in Model.GetErrors(nameof(Model.Name)))
                    {
                        <div class="course-definition-editor__field-error">@error</div>
                    }
                </div>

                <div class="field">
                    <label class="field__label" asp-for="ShortName"></label>
                    <input asp-for="ShortName" autocomplete="off"/>
                    @foreach (var error in Model.GetErrors(nameof(Model.ShortName)))
                    {
                        <div class="course-definition-editor__field-error">@error</div>
                    }
                </div>

                <div class="field course-definition-editor__checkbox-field">
                    <span class="field__label">@Html.DisplayNameFor(x => x.IsActive)</span>
                    <label class="course-definition-editor__checkbox">
                        <input asp-for="IsActive"/>
                        <span>Enable this course definition</span>
                    </label>
                </div>
            </div>

            <div class="field">
                <label class="field__label" asp-for="Description"></label>
                <textarea asp-for="Description" rows="4"></textarea>
                @foreach (var error in Model.GetErrors(nameof(Model.Description)))
                {
                    <div class="course-definition-editor__field-error">@error</div>
                }
            </div>
        </section>
    </div>

    <div class="course-definition-editor__foot">
        <button type="submit" class="is-primary">@Model.SubmitText</button>
        <button type="button"
                hx-get="@cancelUrl"
                hx-target="#course-definitions-content"
                hx-swap="innerHTML">
            Cancel
        </button>
    </div>
</form>
